import React from "react";

const Loading = () => {
  return (
    <div className=" min-h-full min-w-full bg-secondary text-2xl flex flex-col justify-center items-center">
      <p>Loading quetions...</p>
    </div>
  );
};

export default Loading;
